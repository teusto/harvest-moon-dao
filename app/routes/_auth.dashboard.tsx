import styles from '~/styles/dashboard.module.scss'

const Dashboard = (): React.JSX.Element => {
    return (
        <section className={styles.wrapper}>Dashboard</section>
    )
};

export default Dashboard
